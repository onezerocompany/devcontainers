name: Publish Docker Images

on:
  schedule:
    - cron: "0 0 * * 0"
  push:
    branches:
      - main

jobs:
  base-image:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Image
        uses: ./.github/actions/build-image
        with:
          name: base
          platforms: linux/amd64,linux/arm64,linux/arm/v7,linux/arm/v6,linux/386,linux/ppc64le,linux/s390x
          folder: ./base-image

  runner:
    runs-on: ubuntu-latest
    needs: base-image

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Image
        uses: ./.github/actions/build-image
        with:
          name: runner
          folder: ./runner-image

  devcontainer:
    runs-on: ubuntu-latest
    needs: base-image

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Image
        uses: ./.github/actions/build-image
        with:
          name: devcontainer
          folder: ./devcontainer-image
