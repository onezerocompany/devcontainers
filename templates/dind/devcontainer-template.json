{
  "id": "dind",
  "version": "1.0.0",
  "name": "OneZero Docker-in-Docker Dev Container",
  "platforms": ["linux/amd64", "linux/arm64"],
  "keywords": ["onezero", "dind"],
  "features": {},
  "options": {
    "variant": {
      "type": "string",
      "default": "stable",
      "description": "The base image to use for the development container."
    }
  }
}
